<div class="container hero-content">
  <p class="eyebrow">Data Visualization &amp; Analysis</p>
  <h1>Turn complex data into clear, actionable insight.</h1>
  <p class="lead">
    For over a decade, Qualta Consulting has excelled in information visualization.
    That expertise powers the Vorteks software suite — built for analysts, data scientists,
    and teams who need advanced processing, storage, and visualization in one place.
  </p>
</div>

<style>
  .hero-content {
    position: relative;
    max-width: 760px;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: var(--color-accent-hover);
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0 0 1.25rem;
    font-family: var(--font-display);
    font-size: clamp(2.4rem, 5vw, 3.75rem);
    font-weight: 400;
    line-height: 1.08;
    color: #fff;
  }

  .lead {
    margin: 0;
    font-size: 1.12rem;
    line-height: 1.75;
    color: var(--color-text-muted);
    max-width: 62ch;
  }

  @media (max-width: 768px) {
    .lead {
      font-size: 1rem;
    }
  }
</style>